/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  output: 'export',
  basePath: '/gymflow',
  assetPrefix: '/gymflow/',
  trailingSlash: true,
}

module.exports = nextConfig
