# Archived Landing Snapshot

This directory preserves the retired GitHub Pages landing/download site exactly as it existed before the SaaS app was promoted to the repo root on April 24, 2026.

It is reference-only:

- it is not part of the active build or deploy path
- it may contain historical imports and config that are no longer runnable as-is
- any future cleanup or deletion should happen in a separate, explicit change
