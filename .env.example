# Firebase Configuration
# (Optional - if needed for future enhancements)

# Google Cloud Storage Bucket for downloads
# Update the links in app/page.jsx with your actual bucket name
NEXT_PUBLIC_GCS_BUCKET=your-bucket-name
